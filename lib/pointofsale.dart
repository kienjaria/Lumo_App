import 'package:flutter/material.dart';

class PointofSaleTab extends StatelessWidget {
  const PointofSaleTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Point Of Sale'),
      ),
    );
  }
}
